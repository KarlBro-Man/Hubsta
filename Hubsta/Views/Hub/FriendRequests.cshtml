@model IEnumerable<FriendRelation>
@{
}
<h1>Friend Requests</h1>
@foreach(var item in Model)
{
    <div class="container position-relative">
        <div class="row">
            <div class="col">
                <h3>
                    @item.User1.FirstName
                </h3>
            </div>
            <div class="col">
            </div>
        </div>
    </div>
}
